# Dev default API key (override per-machine in Settings, stored in localStorage).
# The app calls the API at its own origin (/api) via the gateway/dev proxy — no base URL needed.
VITE_API_KEY=dev-local-bookmarkfeeder-key
